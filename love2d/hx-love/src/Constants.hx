class Constants {
	public static inline final SCREEN_WIDTH = 240;
	public static inline final SCREEN_HEIGHT = 136;

	public static inline final MAP_CELLS_WIDTH = 8;
	public static inline final MAP_CELLS_HEIGHT = 8;

	public static inline final MAP_REGION_CELLS_WIDTH = 30;
	public static inline final MAP_REGION_CELLS_HEIGHT = 17;

	public static inline final MAP_TOTAL_CELLS_WIDTH = 240;
	public static inline final MAP_TOTAL_CELLS_HEIGHT = 136;

	public static inline final MAP_PX_WIDTH = MAP_CELLS_WIDTH * MAP_REGION_CELLS_WIDTH * 8;
	public static inline final MAP_PX_HEIGHT = MAP_CELLS_HEIGHT * MAP_REGION_CELLS_HEIGHT * 8;

	public static inline final EMPTY_TILE_ID = 0;
}
